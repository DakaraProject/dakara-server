# Configuration file for gunicorn
# `gunicorn.conf.py` is the configuration file that is used and that should be
# maintained by the user
# `gunicorn.conf.py.sample` is the default configuration file which is updated
# automatically
# Version: 1.9.0

workers = 4
